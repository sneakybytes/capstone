kind: Service
apiVersion: v1
metadata:
  labels:
    app: webapp
  name: capstone-service
spec:
  ports:
  - nodePort: 30500
    port: 80
    protocol: TCP
    targetPort: 80
  selector:
    app: web
  type: NodePort
